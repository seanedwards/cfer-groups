require "spec_helper"

describe Cfer::Groups do
  it "has a version number" do
    expect(Cfer::Groups::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
